# 리스트 컴프리헨션

- 짧고 간결하게 리스트를 생성할 수 있는 문법이다. 이는 메모리 사용을 최소화하면서 효율적으로 코드를 작성할 수 있다.

## 기본 구조

```
[표현식 for 요소 in 반복 가능한 객체 if 조건]
```

### 1부터 10까지 정수를 순서대로 가지고 있는 리스트를 생성하는 코드

```
numbers = []
for n in range(1, 11):
    numbers.append(n)
```

```
[x for x in range(10)]